spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/productos
    username: imgamp
    password: liliana00
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8080

app:
  api-key: ${API_KEY:XYZ123}